#
# This file sets up include directories, link directories, and
# compiler settings for a project to use RTToolbox.  It should not be
# included directly, but rather through the RTToolbox_USE_FILE setting
# obtained from RTToolboxConfig.cmake.
#

# Add include directories needed to use RTToolbox.
INCLUDE_DIRECTORIES(BEFORE ${RTToolbox_INCLUDE_DIRS})

# Add link directories needed to use RTToolbox.
LINK_DIRECTORIES(${RTToolbox_LIBRARY_DIRS})

# Add target properties.
INCLUDE(${RTToolbox_TARGETS_FILE})